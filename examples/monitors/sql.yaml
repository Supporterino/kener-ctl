kind: Monitor
metadata:
  tag: sql-example
spec:
  name: SQL Example
  description: "Database connectivity and query execution check"
  type: SQL
  categoryName: Data
  cronSchedule: "*/5 * * * *"
  typeData:
    _type: SQL
    host: db.example.com
    port: 5432
    username: monitor
    password: CHANGEME
    database: app_production
    query: SELECT 1
    timeout: 5000
