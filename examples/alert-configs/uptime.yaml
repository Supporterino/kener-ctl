kind: AlertConfig
metadata:
  name: api-uptime-critical
spec:
  monitorTag: api-example
  alertType: UPTIME
  alertValue: "99.5"
  failureThreshold: 1
  successThreshold: 2
  severity: CRITICAL
  createIncident: true
  triggerNames:
    - ops-slack
    - ops-email
    - ops-discord
